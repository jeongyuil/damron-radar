# 성환·유일 싱크 안건 (2026-09-10)

출처: `snowflake_sample_review_2026-09-09.md` · `golden_bench_2026-09-09.md` · `w2_plan_dictionaries_2026-09.md`

## A. 같이 결정해야 하는 것 (합의 없이는 못 감)

| # | 안건 | 유일 제안 | 왜 합의 사항인가 |
|---|---|---|---|
| A1 | **"사전"의 정의** — 2주차 유일 업무 | 규칙 분류기가 아니라 ① 프롬프트 주입 참조자료 ② 검수 기준 ③ 전사 정규화 테이블. LLM 분류(§6.2·6.3)를 대체하지 않음 | 기대치가 다르면 2주차 산출물이 통째로 어긋남 |
| A2 | **이슈별 stance 부호 규칙** (31건) | issues_seed 정의 문장 기준으로 "+ = 무엇을 지지" 한 줄씩. 초안은 유일, 승인은 성환 | §8 지표 해석 변경 = 상호 승인 대상 |
| A3 | **판례집 → 프롬프트 반영** | 판례집은 data/에 두고, 프롬프트 v2 파일에 주입하는 PR은 상호 승인 | 프롬프트 변경 = 상호 승인 대상(CLAUDE.md) |
| A4 | **댓글 수집** | Phase 1 비범위 + 개인식별자(작성자 핸들·채널ID) → 수집 중단·200건 삭제, Phase 2 재논의 | PRD §1 비범위 항목을 뒤집는 셈이라 결정 로그 필요 |
| A5 | **쇼츠(3분 미만) 처리 — D1 미결** | 전사 생략, 언급량만 집계(decision_log 기본안) | Snowflake 샘플 28%가 쇼츠. 미결이면 전사 비용·전사 테이블 오염 |
| A6 | **전사 보관 위치** | RAW와 분리된 스키마(예: TRANSCRIPTS_TMP) + 짧은 retention | 7/29 합의(자산 DB와 물리적 분리)의 Snowflake 적용 방식 |

## B. 성환님께 요청 (엔지니어 재량 영역, 이유만 공유)

| # | 요청 | 이유 |
|---|---|---|
| B1 | 전사 세그먼트(start/end) 보존 · 10,000자 절단 제거 · transcript_source 컬럼 | 타임스탬프 링크 없으면 Step 3·§8·청킹 스펙 전부 막힘. **최우선** |
| B2 | 수집 대상을 `channels_final_v1.0.csv` 31곳으로 고정, BIAS는 영상 행이 아니라 channels 테이블(register 성향축) | 샘플 9개 채널이 register 밖, 19곳 누락, 중립 축 0 |
| B3 | 3시간 초과 절단 플래그(`truncated`) | §4.3 |
| B4 | ANTHROPIC_API_KEY 발급·공유(dev 키) | 골든셋 벤치에 Haiku 4.5·Sonnet 5(D3 기본안) 행이 비어 있음. 현 Qwen 9B는 type 53.8%·방향 28.6%로 게이트(85/90) 한참 아래 |
| B5 | Snowflake 비밀번호 교체 + 분석가용 롤·사용자 분리 | ACCOUNTADMIN 평문 공유 |
| B6 | 미사용 웨어하우스(LEARNING·STREAMLIT) auto-suspend 또는 삭제, 월 예상 크레딧 한 줄 | 비용 원칙 §12 |
| B7 | claim이 나올 논객 채널 영상 1편 추가 라벨링 | 골든셋 13건에 claim 0건 → type 채점 반쪽. 3주차 평가 입력 겸 |

## C. 유일이 성환님께 확인받을 것

| # | 항목 |
|---|---|
| C1 | 골든셋 stance 라벨 4건(#6·#7 +2, #9 +1, #10 +1) — 어느 이슈 기준의 부호인지. A2와 같이 풀림 |
| C2 | `DESCRIPTION_SUMMARY`가 98/130건에서 원문과 다름 — 무엇으로 만들었는지(LLM이면 model_id·prompt_version 기록 대상) |
| C3 | 전사 20건 선정 기준(23초 쇼츠 3건·2시간 라이브 2건 포함) |

## D. 공유만 (결정 불필요)

- 골든셋 벤치 스크립트 완성: `scripts/golden_bench.py`. 후보 추가는 레지스트리 또는 `--add`. Qwen thinking 모드는 탈락(건당 6~24분·오답).
- 2주차 유일 일정: 9/10~16, 사전 5종 + 골든셋 재채점(전/후 비교). 화요일 PR.
- 3주차 "테스트 데이터 평가"는 B1이 반영된 Snowflake 전사가 있어야 그 데이터로 벤치를 돌릴 수 있음 — B1이 3주차의 선행 조건.

## 우선순위 한 줄

**B1(전사 타임스탬프) → A2(stance 부호) → A1(사전 정의) → B4(API 키)** 순. 나머지는 이번 주 안에.
